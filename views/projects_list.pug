extends layout

block content
  h1= title

  ul
    each project in projects_list
      li 
       a(href=project.url) #{project.name_text} 
       | (#{project.id})

    else
      li There are no projects.

